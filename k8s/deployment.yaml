apiVersion: apps/v1
kind: Deployment
metadata:
  name: bank-app
  namespace: bank-namespace
spec:
  replicas: 4
  selector:
    matchLabels:
      app: bank-app
  template:
    metadata:
      labels:
        app: bank-app
    spec:
      containers:
        - name: bank-app
          image: 172.17.78.58:8000/bank/bank-app:1.0
          ports:
            - containerPort: 8080
